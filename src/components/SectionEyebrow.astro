---
interface Props {
  text: string;
  class?: string;
}
const { text, class: className = '' } = Astro.props;
---
<p class={`font-sans text-sm font-medium tracking-widest uppercase text-sage mb-4 ${className}`}>
  {text}
</p>
